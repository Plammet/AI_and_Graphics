Rotate.cpp
=========================
This code rotates the image by given angle.

To use:
1. put the name of the image file as a parameter of the "imread" on line 14
2. put the angle and interpolation method(nearest, bilinear) in line 26
3. run

You can see original image and rotated image on each screen